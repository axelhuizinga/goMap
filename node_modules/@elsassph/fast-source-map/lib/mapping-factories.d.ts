import { DecodedMapping } from './interfaces';
export declare function createMapping1(col: number): DecodedMapping;
export declare function createMapping4(col: number, src: number, srcLine: number, srcCol: number): DecodedMapping;
export declare function createMapping5(col: number, src: number, srcLine: number, srcCol: number, name: number): DecodedMapping;
