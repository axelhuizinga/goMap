import { DecodedMappings, LineMappings } from './interfaces';
import { Delegate } from './mappings-decoder';
export default class Decoder implements Delegate {
    currentLine: LineMappings;
    mappings: DecodedMappings;
    newline(): void;
    mapping1(col: number): void;
    mapping4(col: number, src: number, srcLine: number, srcCol: number): void;
    mapping5(col: number, src: number, srcLine: number, srcCol: number, name: number): void;
}
